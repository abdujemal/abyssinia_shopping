import 'package:flutter/material.dart';

class Category {
  String CategoryAmharicName,
      CategoryImage,
      CategoryName,
      main_category,
      productNumber;

  Category(this.CategoryAmharicName, this.CategoryImage, this.CategoryName,
      this.main_category, this.productNumber);
}
